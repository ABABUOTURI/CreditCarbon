function buyToken(amount) {
    // Simulate buying tokens (you would replace this with actual backend interaction)
    console.log(`Buying ${amount} tokens`);
    alert(`Successfully bought ${amount} tokens!`);
}
